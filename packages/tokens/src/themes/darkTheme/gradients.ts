import { colors } from './colors'

export const gradients = {
  primary: `linear-gradient(${colors.primary}, ${colors.primary})`,
  soft: 'linear-gradient(253.3deg, #f0e9fd 0%, #e9ecf7 49.56%, #e9f7f3 100%)',
}
